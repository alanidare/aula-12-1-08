import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({

 container: {
    justifyContent: 'center',
    flexDirection: "row",
    alignItems: 'center',
    display: 'flex',
    padding: 16,
    backgroundColor: '#3d0a4d',
    borderRadius: 8,
 },

 title:{

 },

})